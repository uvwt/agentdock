.DS_Store
/workspace/
/AgentDock/

# Local build/runtime artifacts.
# The repository root may contain the currently installed macOS host binary
# and timestamped rollback copies. Keep them local; release artifacts belong
# under dist/ or an external release system, not in git.
/agentdock
/agentdock.new
/agentdock.new.*
/agentdock.prev
/agentdock.prev.*
/agentdock.bak*
/bin/
/dist/
/coverage.out
/*.test
/agentdock.killed*
