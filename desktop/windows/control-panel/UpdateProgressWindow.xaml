<Window x:Class="AgentDock.ControlPanel.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AgentDock.ControlPanel"
        Title="{local:Loc UpdateWindowTitle}"
        Width="460"
        MinWidth="460"
        MinHeight="220"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               FontSize="17"
               FontWeight="SemiBold"
               Text="{local:Loc UpdatingAgentDock}" />
    <TextBlock x:Name="VersionText"
               Grid.Row="1"
               Margin="0,10,0,0"
               Foreground="#5F6368" />
    <TextBlock x:Name="StatusText"
               Grid.Row="2"
               Margin="0,18,0,8"
               Text="{local:Loc PreparingUpdate}"
               TextWrapping="Wrap" />
    <ProgressBar x:Name="UpdateProgressBar"
                 Grid.Row="3"
                 Height="18"
                 Minimum="0"
                 Maximum="100"
                 Value="0" />
    <Button x:Name="CloseButton"
            Grid.Row="4"
            Width="80"
            Margin="0,20,0,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Content="{local:Loc Close}"
            IsEnabled="False"
            Click="CloseButton_Click" />
  </Grid>
</Window>
